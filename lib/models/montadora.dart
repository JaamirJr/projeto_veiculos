import 'package:flutter/material.dart';

class Montadora  extends StatelessWidget {
  final String id;
  final String nome;
  final Color cor;

  const Montadora({
    required this.id,
    required this.nome,
    this.cor = Colors.orange
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}